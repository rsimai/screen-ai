OLLAMA_SERVER_URL = 'http://yourhost:11434' 
#MODEL_NAME = 'llama3.2-vision'
MODEL_NAME = 'granite3.2-vision'
SCREENSHOT_COMMAND = 'spectacle -b -r -n -o /tmp/$FILENAME'
INITIAL_PROMPT = """You are a QA assistant. Your task is to analyze the provided image.

**Step 1: OCR**
First, try to extract all text from the image, but don't output. If there's no text then focus on the image and identify details.

**Step 2: Analysis**
Review the extracted text or image details for any mistakes, errors, or anomalies.

**Step 3: Reporting**
- If you find any errors, describe them clearly.
- If you find no errors, briefly summarize.

Do not include any other commentary or introductory phrases."""
