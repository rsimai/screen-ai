OLLAMA_SERVER_URL = 'http://yourhost:11434' 
#MODEL_NAME = 'llama3.2-vision'
MODEL_NAME = 'granite3.2-vision'
SCREENSHOT_COMMAND = 'spectacle -b -r -n -o /tmp/$FILENAME'
INITIAL_PROMPT = """You are a QA assistant. Your task is to analyze the provided image.

**Step 1: OCR**
First, extract all text from the image, preserving the original formatting as much as possible.

**Step 2: Analysis**
Review the extracted text for any mistakes, errors, or anomalies.

**Step 3: Reporting**
- If you find any errors, describe them clearly but briefly.
- If you find no errors, simply respond with exactly one sentence that shows what you're seeing.

Do not include any other commentary or introductory phrases."""
